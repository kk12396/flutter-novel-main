# NovelGenerator - 用户使用手册

## 软件简介

NovelGenerator 是一款 AI 智能小说创作工具，通过对接多种大语言模型（LLM），帮助作者自动生成小说大纲、章节规划和正文内容，提升创作效率。

## 系统要求

### Windows 版本
- **操作系统**: Windows 10/11 (64位)
- **内存**: 建议 4GB 以上
- **存储空间**: 至少 1GB 可用空间
- **网络**: 需要网络连接（使用在线模型时）

### Android 版本
- **系统版本**: Android 8.0 及以上
- **内存**: 建议 4GB 以上
- **存储空间**: 至少 1GB 可用空间
- **网络**: 需要网络连接（使用在线模型时）

## 快速开始

### 1. 启动软件

**Windows**: 双击运行 `novelgenerator.exe`，进入主界面。

**Android**: 安装 `app-release.apk` 后，点击应用图标启动。

### 2. 切换主题

软件支持浅色/深色主题切换：
- 在主界面点击 **"深色主题"** / **"浅色主题"** 按钮
- 或在创作界面点击顶部工具栏的月亮/太阳图标
- 主题设置会自动保存

### 3. 配置 AI 模型

首次使用需要配置 AI 模型：

1. 点击界面上的 **"AI配置"** 按钮
2. 选择提供商（默认使用 OpenRouter）
3. 输入 API Key（从对应平台获取）
4. 选择模型（推荐使用免费模型）
5. 点击 **"保存配置"**

**支持的免费模型（OpenRouter）**:
- `nvidia/nemotron-3-super-120b-a12b:free` (默认)
- `minimax/minimax-m2.5:free`
- `stepfun/step-3.5-flash:free`
- `arcee-ai/trinity-large-preview:free`
- `arcee-ai/trinity-mini:free`
- `z-ai/glm-4.5-air:free`

### 4. 创建新项目

1. 在主界面点击 **"新建项目"**
2. 输入项目名称（如：我的武侠小说）
3. 点击 **"创建"**

### 5. 生成小说大纲

1. 在 **"大纲"** 标签页
2. 输入小说的基本信息提示
3. 点击 **"生成大纲"** 按钮
4. 等待 AI 生成完整大纲（700-900字精简格式）

### 6. 生成分卷规划

1. 切换到 **"分卷规划"** 标签页
2. 点击 **"生成分卷规划"** 按钮
3. 查看生成的分卷规划内容

### 7. 生成章节规划

1. 切换到 **"章节规划"** 标签页
2. 设置起始章节和生成数量
3. 点击 **"生成章节规划"** 按钮
4. 查看生成的章节规划列表

### 8. 生成章节正文

1. 切换到 **"生成章节"** 标签页
2. 输入要生成的章节编号
3. 点击 **"生成章节"** 按钮
4. 等待 AI 生成正文（约 2500 字）
5. 内容会自动保存到 `正文/第X章.txt`

### 9. 智能续写

点击 **"智能续写"** 按钮，软件会自动：
- 检查当前项目状态
- 自动完成缺失的规划步骤
- 按顺序生成后续章节
- 每章生成后自动保存

点击 **"停止"** 按钮可随时停止自动续写。

## 文件说明

### Windows 版本
软件会在运行目录下创建 `books/` 文件夹，每个项目包含：

```
books/项目名称/
├── 大纲.txt              # 小说大纲（700-900字）
├── 分卷规划.txt          # 分卷详细规划
├── 范围规划_第X卷.txt    # 每卷范围规划
├── 进度追踪.txt          # 创作进度追踪
├── 章节规划/             # 章节规划文件夹
│   ├── 第1章_章节规划.txt
│   ├── 第2章_章节规划.txt
│   └── ...
└── 正文/                 # 章节正文文件夹
    ├── 第1章.txt
    ├── 第2章.txt
    └── ...
```

### Android 版本
Android 版本会优先尝试使用外部存储根目录 `/storage/emulated/0/novelgenerator/`，如果权限不足则使用应用私有目录 `/storage/emulated/0/Android/data/com.example.novelgenerator/files/`。

**注意**: 如果使用应用私有目录，需要通过系统文件管理器或连接电脑才能访问文件。

## 创作流程建议

### 推荐步骤

1. **构思阶段**
   - 准备小说的基本构思（题材、主角、世界观）
   - 使用 "生成大纲" 创建完整大纲

2. **规划阶段**
   - 查看生成的大纲，必要时手动调整
   - 生成分卷规划
   - 生成章节规划（建议先生成 10-20 章）

3. **写作阶段**
   - 使用 "智能续写" 自动生成章节
   - 或手动逐章生成并查看效果
   - 每章生成后会自动保存

4. **完善阶段**
   - 在 `正文/` 文件夹中查看生成的章节
   - 手动编辑调整不满意的部分
   - 继续生成后续章节

## 进度追踪

软件会自动维护进度追踪，包含：
- **时间线**: 当前天数和时间
- **场景**: 当前位置和状态
- **设定状态**: 技能、道具、职业变化
- **关键角色**: 好感度、关系、位置
- **最近章节**: 结尾场景和关键事件
- **逻辑约束**: 需要遵守的设定

进度追踪会参与下一章的生成，确保故事连贯性。

## 使用本地模型（Ollama）

如需使用本地模型：

1. 安装 Ollama: https://ollama.com
2. 下载模型: `ollama pull llama3` 或 `ollama pull qwen`
3. 在软件中选择 "Ollama" 提供商
4. 确保 Base URL 为 `http://localhost:11434`
5. 选择已下载的模型

**注意**: 本地模型需要较强的硬件配置。

## 常见问题

### Q: 生成章节时提示超时？

A: 
- 大型模型（如 120B）生成较慢，已设置 180 秒超时
- 建议切换到响应更快的模型（如 step-3.5-flash）
- 检查网络连接是否稳定

### Q: 生成的内容包含英文？

A: 
- 提示词已要求全部使用中文
- 如仍有英文，可手动编辑或在提示中再次强调

### Q: 如何修改已生成的内容？

**Windows**:
- 在 `books/项目名称/正文/` 文件夹中找到对应章节
- 使用文本编辑器直接修改

**Android**:
- 如果使用外部存储目录 `/storage/emulated/0/novelgenerator/`，可以使用手机文件管理器访问
- 如果使用应用私有目录，需要连接电脑或使用系统文件管理器访问 `/storage/emulated/0/Android/data/com.example.novelgenerator/files/`

### Q: 智能续写从哪里开始？

A: 
- 软件会自动检测项目状态
- 按顺序：大纲 → 分卷规划 → 范围规划 → 章节规划 → 章节正文
- 从第一个缺失的步骤开始

### Q: 可以导出小说吗？

**Windows**:
- 章节正文保存在 `正文/` 文件夹中
- 可直接复制内容到 Word 等编辑器

**Android**:
- 章节正文保存在 `/storage/emulated/0/novelgenerator/books/项目名称/正文/` 或应用私有目录中
- 可以通过文件管理器复制内容

### Q: 大纲生成后可以重新生成吗？

A: 
- 大纲一旦生成就固定，不支持重新生成或继续完善
- 如需重新规划大纲，请创建新项目

## 注意事项

1. **API Key 安全**: API Key 保存在本地，请勿分享配置文件
2. **网络使用**: 使用在线模型需要网络连接，本地模型不需要
3. **存储空间**: 长篇小说会占用较多存储空间，请确保磁盘空间充足
4. **内容审核**: AI 生成内容请自行审核，确保符合相关法律法规

## 技术支持

如有问题或建议，请联系开发团队。

## 更新日志

### v1.3.0
- 简化创作流程：合并写作大纲和精简大纲为单一大纲
- 移除重新生成和继续完善功能，生成后固定内容
- 优化文件结构，减少冗余文件
- 删除分卷概要字段，简化数据模型
- 更新提示词模板，生成更精简的大纲格式

### v1.2.0
- 新增 Android 版本支持
- 新增系统设置功能（主题、自动续写延迟）
- 优化存储路径，支持外部存储和应用私有目录自动切换
- 修复 Windows 构建问题

### v1.1.0
- 新增深色/浅色主题切换功能
- 优化界面布局，按钮位置调整
- 修复章节列表加载问题
- 改进深色主题下的界面元素显示

### v1.0.0
- 初始版本发布
- 支持多种 LLM 提供商
- 完整的小说创作流程
- 智能续写功能
- 进度追踪系统
